import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["picsum.photos", "images.unsplash.com", "avatars.githubusercontent.com"],
  }
};

export default nextConfig;
